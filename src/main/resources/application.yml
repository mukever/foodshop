spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://127.0.0.1:3306/food_project?characterEncoding=utf-8&useSSL=false
  jpa:
    show-sql: true
  jackson:
    default-property-inclusion: non_null
  http:
    encoding:
      force: true
      charset: UTF-8
  freemarker:
    allow-request-override: false
    cache: false
    check-template-location: true
    charset: UTF-8
    content-type: text/html; charset=utf-8
    expose-request-attributes: false
    expose-session-attributes: false
    expose-spring-macro-helpers: false
    suffix: .ftl
    template-loader-path: classpath:/templates

    #  mvc:
    #   view:
    #     prefix: /WEB-INF/
    #     suffix: .jsp
    #  mvc:
    #   view:
    #     prefix: /templates/
    #     suffix: .ftl

   #redis配置
#  redis:
#      host: localhost
#      port: 6379
#      # REDIS (RedisProperties)
#      # Redis数据库索引（默认为0）
#      database: 0
#      # Redis服务器连接密码（默认为空）
#      password:
#      # 连接池最大连接数（使用负值表示没有限制）
#      # 连接超时时间（毫秒）
#      timeout: 0
#      pool:
#        max-active: 8
#        # 连接池最大阻塞等待时间（使用负值表示没有限制）
#        max-wait: -1
#        # 连接池中的最大空闲连接
#        max-idle: 8
#        # 连接池中的最小空闲连接
#        min-idle: 0
server:
    context-path: /foodshop






